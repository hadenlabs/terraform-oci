# Prompt: Crear módulo OCI – VCN

## Contexto

Estás trabajando dentro del repositorio `terraform-oci`.

Debes cumplir estrictamente todas las reglas definidas en `AGENTS.md`. Si existe conflicto entre este prompt y `AGENTS.md`, prevalece `AGENTS.md`.

No se permite introducir nuevas convenciones arquitectónicas.

El módulo debe replicar exactamente la arquitectura utilizada en:

```
modules/compute/
```

El módulo debe integrarse correctamente con:

- provision/generators/README.yaml
- Taskfile.yml raíz

---

## Objetivo

Crear un módulo Terraform para el recurso:

```
oci_core_vcn
```

Ubicación obligatoria:

```
modules/vcn/
```

El módulo debe seguir exactamente la arquitectura utilizada en:

```
modules/compute/
```

No se permite introducir nuevas convenciones estructurales.

---

## Integración Obligatoria con el Proyecto

El agente debe asegurarse de que:

1. El módulo quede listado en: provision/generators/README.yaml

   Debe agregarse dentro de:

   modules:
   - "Compartment [VCN](modules/vcn/README.md)"

2. El Taskfile.yml raíz incluya:

   module_vcn: ./modules/vcn/Taskfile.yml

3. En la tarea global de generación de documentación se ejecute:
   - terraform:doc con TERRAFORM_PATH=modules/vcn
   - module_vcn:readme

No omitir esta integración.

---

## Arquitectura Obligatoria (main.tf)

Debe incluir:

### 1. locals con 4 bloques exactos

```
locals {
  defaults
  input
  generated
  outputs
}
```

No se permiten estructuras alternativas.

---

### Resource

```hcl
resource "oci_core_vcn" "this"
```

Debe:

- Usar `count = local.outputs.module_enabled ? 1 : 0`
- Consumir exclusivamente `local.outputs.*`
- No usar `var.*` directamente en el resource

---

# Documentación (Sistema Gomplate Obligatorio)

El archivo `README.md`:

- Es generado automáticamente
- NO debe escribirse manualmente
- Debe contener el header estándar del proyecto:

```
DO NOT EDIT THIS FILE
1) Make all changes to `provision/generators/README.yaml`
2) Run `task readme`
```

Fuente de verdad:

```
modules/vcn/README.yaml
```

Y se integra con:

```
provision/generators/README.yaml
```

La carpeta de documentación es:

```
modules/vcn/docs/
```

---

## README.yaml (del módulo)

Debe seguir exactamente el patrón de `modules/compute/README.yaml`.

Cambios obligatorios:

```yaml
name: vcn

tags:
  - oci
  - terraform
  - vcn
```

La descripción debe explicar:

- Qué es un Virtual Cloud Network (VCN) en OCI
- Que es el límite lógico principal de red
- Que contiene subnets públicas y privadas
- Soporte para múltiples CIDR blocks
- Relación con Internet Gateway, NAT Gateway y DRG
- Base para arquitectura de red en OCI

Debe incluir:

```yaml
usages:
  - modules/vcn/docs/usage.md

include:
  - modules/vcn/docs/include/terraform.md
```

---

## usage.md

Debe incluir:

1. Ejemplo básico
2. Ejemplo con múltiples CIDR blocks
3. Ejemplo integrando con módulo subnet
4. Uso del output id como input en otro módulo
5. Ejemplo con module_enabled dinámico

No inventar recursos inexistentes.

---

## include/terraform.md

Crear archivo vacío en:

```
modules/vcn/docs/include/terraform.md
```

## contributing.md

Crear archivo en:

```
modules/vcn/docs/contributing.md
```

Contenido obligatorio:

```
# How To Contribute

Contributions to module user are welcome.

See [Contributing](/docs/contributing.md)
```

---

## Especificación del Recurso

### Requeridos

- compartment_id (string)
- cidr_blocks (list(string))
- display_name (string)

### Opcionales

- dns_label (string)
- defined_tags (map(string))
- freeform_tags (map(string))
- is_ipv6enabled (bool)

### Configuración del módulo

- module_enabled (bool, default = true)

---

# Estructura de Archivos Obligatoria

```
modules/vcn/
 ├── main.tf
 ├── variables.tf
 ├── outputs.tf
 ├── versions.tf
 ├── README.yaml
 ├── README.md  (generado automáticamente)
 ├── docs/
 │    ├── usage.md
 │    ├── contributing.md
 │    └── include/
 │         └── terraform.md
 └── Taskfile.yml
```

No agregar archivos adicionales.

---

# Taskfile.yml (del módulo)

Debe usar rutas específicas al módulo vcn.

No debe contener referencias a compute.

```yaml
---
version: "3"

vars:
  MODULE_PATH: modules/vcn

tasks:
  readme:
    run: once
    desc: Generate Readme
    silent: true
    cmds:
      - >-
        gomplate --file {{.README_TEMPLATE}} --out {{.MODULE_PATH}}/README.md --datasource config={{.MODULE_PATH}}/README.yaml --datasource includes={{.README_INCLUDES}}


  test:
    desc: test module
    silent: true
    cmds:
      - >-
        go test -race -v ./{{.MODULE_PATH}}/... -coverprofile cover.out -timeout 60m
```

---

# Validaciones Específicas para VCN

El README debe incluir sección explícita sobre:

1. CIDR blocks válidos y no superpuestos
2. Relación jerárquica VCN → Subnet
3. Límite regional (no asociado a Availability Domain)
4. Interacción con Internet Gateway, NAT Gateway y DRG
5. DNS label y resolución interna

---

# Formato de Respuesta Obligatorio

El agente debe responder en este orden:

1. Estructura del módulo
2. Contenido completo de cada archivo
3. Checklist de validación contra AGENTS.md
4. Checklist de integración con README raíz
5. Comandos para probar el módulo

Cada archivo debe mostrarse en bloque de código separado con lenguaje correcto.

- No resumir código.
- No omitir archivos.
- No explicar teoría innecesaria.
